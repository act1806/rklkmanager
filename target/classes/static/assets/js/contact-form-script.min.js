$("#contactForm").validator().on("submit", function (event) {
    if (event.isDefaultPrevented()) {
        // handle the invalid form...
        formError();
        submitMSG(false, "请正确填写表单！");
    } else {
        // everything looks good!
        event.preventDefault();
        submitForm();
    }
});

//jquery form序列化转换为json对象
(function($){
    $.fn.serializeJson=function(){
      var serializeObj={};
      var array=this.serializeArray();
      var str=this.serialize();
      $(array).each(function(){
        if(serializeObj[this.name]){
          if($.isArray(serializeObj[this.name])){
            serializeObj[this.name].push(this.value);
          }else{
            serializeObj[this.name]=[serializeObj[this.name],this.value];
          }
        }else{
          serializeObj[this.name]=this.value;
        }
      });
      return serializeObj;
    };
})(jQuery);

function submitForm(){
    // Initiate Variables With Form Content
    // var name = $("#name").val();
    // var email = $("#email").val();
    // var msg_subject = $("#msg_subject").val();
    // var message = $("#message").val();


    // $.ajax({
    //     type: "POST",
    //     url: "assets/php/form-process.php",
    //     data: "name=" + name + "&email=" + email + "&msg_subject=" + msg_subject + "&message=" + message,
    //     success : function(text){
    //         if (text == "success"){
    //             formSuccess();
    //         } else {
    //             formError();
    //             submitMSG(false,text);
    //         }
    //     }
    // });
    var d = $("#contactForm").serializeJson();
    console.log(d);
    console.log(d['content']);
    console.log(d['content'] == undefined);
    console.log(d['content'] == null);
    console.log(d['content'].length);
    if(d['content'] == undefined || d['content'] == null || d['content'].length == 0) {
        return;
    }
    //alert($("#contactForm").serializeJson());
    $.ajax({
        //几个参数需要注意一下
            type: "POST",//方法类型
            dataType: "json",//预期服务器返回的数据类型
            url: "http://www.bj-rklk.com/message/save" ,//url
            data: JSON.stringify($("#contactForm").serializeJson()),
            contentType: "application/json;charset-UTF-8",
            success : function(text){
                if (text == true){
                    formSuccess();
                } else {
                    formError();
                    submitMSG(false,text);
                }
            },
            error : function() {
                alert("异常！");
            }
        });
}

function formSuccess(){
    $("#contactForm")[0].reset();
    submitMSG(true, "留言成功!")
}

function formError(){
    $("#contactForm").removeClass().addClass('shake animated').one('webkitAnimationEnd mozAnimationEnd MSAnimationEnd oanimationend animationend', function(){
        $(this).removeClass();
    });
}

function submitMSG(valid, msg){
    if(valid){
        var msgClasses = "h3 text-left tada animated text-success";
    } else {
        var msgClasses = "h3 text-left text-danger";
    }
    $("#msgSubmit").removeClass().addClass(msgClasses).text(msg);
}